# Implementation Plan: Shared Design Tokens

**Branch**: `063-shared-design-tokens` | **Date**: 2026-03-01 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/063-shared-design-tokens/spec.md`

## Summary

Harmoniser les design tokens entre Angular (SCSS) et Flutter (Dart) via un document de reference unique (`docs/design-tokens.md`). Ajouter la couleur secondaire Indigo (#4F46E5), resoudre les divergences existantes selon une approche best-of-both (cf. research.md), et implementer le support reduced-motion dans Flutter.

## Technical Context

**Language/Version**: TypeScript 5.9 (Angular SCSS), Dart >= 3.6 (Flutter)
**Primary Dependencies**: Angular 21 (SCSS tokens), Flutter >= 3.27 (Dart constants + ThemeData)
**Storage**: N/A (fichiers statiques de configuration)
**Testing**: `ng build` (compilation Angular), `flutter analyze` + `flutter test` (Flutter)
**Target Platform**: Web PWA (Angular) + iOS/Android (Flutter)
**Project Type**: Design system cross-platform (fichiers de configuration)
**Performance Goals**: N/A (tokens statiques, pas de runtime impact)
**Constraints**: WCAG AA contrast ratios (4.5:1 text, 3:1 graphiques), valeurs identiques entre les 3 artefacts
**Scale/Scope**: ~48 primitives couleur, ~39 tokens semantiques x2 themes, 13 spacing, 7 font sizes, 6 radius, 4 shadows, 6 animations

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| # | Principle | Status | Notes |
|---|-----------|--------|-------|
| I | API-First | **N/A** | Aucun endpoint backend. Feature purement frontend (fichiers de config visuels). |
| II | Securite par defaut | **N/A** | Aucune donnee sensible, aucun flux d'auth modifie. |
| III | Simplicite & YAGNI | **PASS** | Modification de fichiers existants uniquement. Pas de build pipeline, pas d'outil de sync automatique. 1 document reference + mise a jour des fichiers existants. |
| IV | Mobile-First UX | **PASS** | Tokens identiques = experience coherente mobile/web. Aucune degradation UX. |
| V | Testabilite | **PASS** | Verification par compilation (build) + comparaison de valeurs. Tests Flutter existants continuent de passer. |
| VI | Observabilite | **N/A** | Aucun changement runtime, pas de logs. |
| VII | Self-Hosted Ready | **N/A** | Aucune dependance infra ajoutee. |

**Gate result: PASS** — Aucune violation. Feature de configuration pure, alignee avec le principe de simplicite.

### Post-Phase 1 Re-check

Confirme : aucune violation introduite par le design. Les modifications sont strictement limitees aux fichiers de tokens/constantes existants + 1 nouveau document Markdown.

## Project Structure

### Documentation (this feature)

```text
specs/063-shared-design-tokens/
├── plan.md              # This file
├── research.md          # Best-of-both decisions, Indigo palette, reduced-motion
├── data-model.md        # Token taxonomy and complete value reference
├── quickstart.md        # Implementation guide
└── tasks.md             # (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
docs/
└── design-tokens.md              # NEW — document de reference unique (FR-001)

app/src/styles/
├── tokens/
│   ├── _primitives.scss           # UPDATE — +Indigo palette, +space-9/11, +radius-xxl, +subscription violet
│   └── _tokens.scss               # UPDATE — +CSS vars Indigo, +space-9/11, +radius-xxl
└── themes/
    ├── _light.scss                # UPDATE — +secondary tokens, harmonise business colors
    └── _dark.scss                 # UPDATE — +secondary tokens, harmonise business colors

flutter/lib/src/
├── constants/
│   ├── app_colors.dart            # UPDATE — +Indigo palette, fix warning, harmonise business/feedback
│   ├── app_typography.dart        # UPDATE — +line heights, +fontMono
│   ├── app_shadows.dart           # UPDATE — harmonise avec reference Angular
│   └── app_durations.dart         # UPDATE — +resolve() reduced-motion, +easing curves renames
├── theme/
│   ├── app_theme.dart             # UPDATE — Indigo→ColorScheme.secondary, fix hardcoded, fix error dark
│   └── app_theme_extension.dart   # UPDATE — harmonise 5 business colors, +secondaryColor
├── constants/app_spacing.dart     # UNCHANGED (deja complet)
└── constants/app_radius.dart      # UNCHANGED (deja xxl)
```

**Structure Decision**: Pas de nouveaux fichiers source. Modification des fichiers existants + 1 document Markdown de reference. Aucun nouveau module, aucune nouvelle dependance.

## Complexity Tracking

Aucune violation de constitution a justifier. Tableau vide.

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| — | — | — |
