{
  "schemaVersion": 1,
  "contractId": "DEMO-005:standard:implement:v1",
  "issueId": "DEMO-005",
  "profile": "standard",
  "activity": "implement",
  "objective": "Ajouter dans l’application frontend un test automatisé de non-régression pour app/nginx.conf. Le test doit vérifier que les requêtes /api/, notamment /api/bank-logos/*.svg, restent prioritaires sur la regex générique des assets SVG grâce à location ^~ /api/, que le proxy_pass vers le conteneur API est conservé, et qu’une régression de cette configuration fait échouer le test. Ne pas modifier le comportement applicatif ni nécessiter un serveur de production.",
  "featurePath": ".specify/specs/106-nginx-validation-cwd",
  "inputs": [
    ".specify/specs/106-nginx-validation-cwd/assessment.json",
    ".specify/specs/106-nginx-validation-cwd/workflow-state.json",
    ".specify/specs/106-nginx-validation-cwd/prepare-result.json"
  ],
  "allowedActions": [
    "read",
    "write",
    "execute"
  ],
  "constraints": [
    "Respecter strictement l'objectif et le perimetre du contrat.",
    "Preserver les changements utilisateur sans rapport avec cette activite.",
    "Ne pas modifier state.json, workflow-state.json ou assessment.json.",
    "Ne pas creer de commit et ne pas changer de branche.",
    "Signaler tout blocage au lieu de contourner une validation.",
    "Limiter les modifications source au changement demande.",
    "Suivre les conventions et les tests existants du depot."
  ],
  "completionCriteria": [
    "Le changement demande est implemente dans le depot.",
    "Les fichiers modifies sont listes dans le resultat.",
    "Les risques ou blocages restants sont explicites."
  ],
  "requiredEvidence": [],
  "expectedOutput": {
    "type": "object",
    "required": [
      "schemaVersion",
      "contractId",
      "status",
      "summary",
      "changedFiles",
      "checks",
      "evidence",
      "remainingRisks",
      "blockers"
    ],
    "properties": {
      "schemaVersion": {
        "type": "integer",
        "const": 1
      },
      "contractId": {
        "type": "string"
      },
      "status": {
        "type": "string",
        "enum": [
          "completed",
          "blocked"
        ]
      },
      "summary": {
        "type": "string"
      },
      "changedFiles": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "checks": {
        "type": "array",
        "items": {
          "type": "object",
          "required": [
            "command",
            "exitCode",
            "status"
          ],
          "properties": {
            "command": {
              "type": "string"
            },
            "exitCode": {
              "type": "integer"
            },
            "status": {
              "type": "string",
              "enum": [
                "passed",
                "failed"
              ]
            }
          },
          "additionalProperties": false
        }
      },
      "evidence": {
        "type": "array",
        "items": {
          "type": "object",
          "required": [
            "id",
            "outcome",
            "report"
          ],
          "properties": {
            "id": {
              "type": "string"
            },
            "outcome": {
              "type": "string",
              "enum": [
                "passed",
                "failed"
              ]
            },
            "report": {
              "type": "string"
            }
          },
          "additionalProperties": false
        }
      },
      "remainingRisks": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "blockers": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    },
    "additionalProperties": false
  }
}
