spring:
  application:
    name: budget-api
  profiles:
    active: dev

  flyway:
    enabled: true

  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    expiration: 86400000

server:
  port: 8080
  servlet:
    context-path: /api
