export const environment = {
  production: false,
  // Racine de l'API, hors versionnement : actuator, ressources statiques.
  apiRootUrl: '/api',
  // Endpoints metier, versionnes (KKS-313).
  apiUrl: '/api/v1',
};
